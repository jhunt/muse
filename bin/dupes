#!/usr/bin/perl

use strict;
use warnings;
use Muse;

my %shas = ();
for (provided_files) {
	s/\0//g;
	print ">> $_\n";
	my $sha = waveform_checksum($_);
	push @{ $shas{$sha} }, $_;
}

use Data::Dumper;
print Dumper(\%shas);
